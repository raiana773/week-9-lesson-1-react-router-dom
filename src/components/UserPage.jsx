import React, { useEffect, useState } from "react";

const UserPage = () => {
  const [user, setUsers] = useState([]);

  useEffect(() => {
    fatch("");
  });
  return (
    <div>
      <h1>Users Page</h1>
    </div>
  );
};

export default UserPage;
